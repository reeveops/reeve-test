name: Reeve Shared Drift E2E

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read
  issues: write
  id-token: write

jobs:
  drift:
    uses: reeveops/reeve/.github/workflows/reeve.yml@de1bdde7c5c68aa75fb88485aede113b131acd26
    with:
      mode: drift
      root: tf
      opentofu_version: "1.12.6"
