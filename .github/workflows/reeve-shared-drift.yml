name: Reeve Shared Drift E2E

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read
  issues: write
  id-token: write

jobs:
  drift:
    uses: reeveops/reeve/.github/workflows/reeve.yml@8970a3ef79348a121635f031dcf8bb740ecf5dbd
    with:
      mode: drift
      root: tf
      opentofu_version: "1.12.6"
