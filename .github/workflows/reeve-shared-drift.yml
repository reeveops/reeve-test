name: Reeve Shared Drift E2E

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read
  issues: write
  id-token: write

jobs:
  drift:
    uses: reeveops/reeve/.github/workflows/reeve.yml@ec917c9ab62e0052b5d3376897cfe84aa647b17f
    with:
      mode: drift
      root: tf
      opentofu_version: "1.12.6"
