name: Reeve Shared Drift E2E

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read
  issues: write
  id-token: write

jobs:
  drift:
    uses: reeveops/reeve/.github/workflows/reeve.yml@64461d8358885bbbe999c44288741736a017d5b7
    with:
      mode: drift
      root: tf
      opentofu_version: "1.12.6"
