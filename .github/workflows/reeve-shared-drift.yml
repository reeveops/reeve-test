name: Reeve Shared Drift E2E

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read
  issues: write
  id-token: write

jobs:
  drift:
    uses: reeveops/reeve/.github/workflows/reeve.yml@5ef3fa3910a38775905e7048d38fbb86498c6c82
    with:
      mode: drift
      root: tf
      opentofu_version: "1.12.6"
