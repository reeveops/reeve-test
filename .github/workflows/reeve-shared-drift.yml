name: Reeve Shared Drift E2E

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read
  issues: write
  id-token: write

jobs:
  drift:
    uses: reeveops/reeve/.github/workflows/reeve.yml@0fc118139931fde27444ddc3d8527b16fbc381d5
    with:
      mode: drift
      root: tf
      opentofu_version: "1.12.6"
