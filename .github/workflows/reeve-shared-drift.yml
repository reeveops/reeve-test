name: Reeve Shared Drift E2E

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read
  issues: write
  id-token: write

jobs:
  drift:
    uses: reeveops/reeve/.github/workflows/reeve.yml@13b065f2c862745869cc657d9e872de049eece7d
    with:
      mode: drift
      root: tf
      opentofu_version: "1.12.6"
