# Pulumi local-file backend (created by `pulumi login --local`).
.pulumi/
pulumi-state/

# Reeve local-filesystem state bucket (root and tf/ roots).
.reeve-state/

# Terraform / OpenTofu local state and plugin caches (tf/ subtree).
.terraform/
*.tfstate
*.tfstate.*
terraform.tfstate.d/

# Go build artefacts.
/bin/
/dist/

# Local override / scratch.
.local/
e2e-report/
*.local.yaml
gha-creds-*.json

# IDE.
.idea/
.vscode/
